Execute a PowerShell command (pwsh -Command) and return its stdout/stderr. Each call runs in a fresh pwsh process: no state (cwd, vars, functions) persists — pass workdir instead of using cd. Paths use native Windows form (C:\...); read environment variables with $env:NAME. Non-zero exits are reported as [exit code: N]. Harness environment facts are exposed through managed $env:DSH_* variables; inspect them when needed. Commands may run under a file sandbox; a blocked file operation is reported as [sandbox: file access denied under <mode> mode] — a policy denial, not a bug; do not retry another way. Long output is truncated to its tail; the full output is saved to a file whose path is reported when available. On Windows a force-killed command settles as [exit code: 1] without a signal marker — treat it as an interruption, not a command failure. Set run_in_background: true for long-running commands: it returns a job id; read output with job_output (action='kill' to stop it).

Quick reference:
- Cmdlets use Verb-Noun names: Get-ChildItem, Get-Content, Set-Location, Copy-Item, Move-Item, Remove-Item, New-Item, Select-String.
- Prefer glob/grep over Get-ChildItem/Select-String for search.
- Use `| Out-Null` or `> $null`, not `> nul`, to discard output — `> nul` would create an empty file named `nul` in PowerShell.
